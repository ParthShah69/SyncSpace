# Production environment — loaded automatically by Vite during `vite build`
# Local dev still uses .env (localhost values)
VITE_API_URL=https://syncspace-api.onrender.com/api
VITE_SOCKET_URL=https://syncspace-api.onrender.com
